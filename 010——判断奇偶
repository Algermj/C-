#include<stdio.h>
void main()
{
   while(1)
   {
      int  a;
      printf("输入一个数：");
      scanf("%d",&a);
	  if(a%2==0)
	  {
		  printf("%d是偶数\n",a);
	  }
	  else 
	  {
		  printf("%d是奇数\n",a);
	  }
   }

}
